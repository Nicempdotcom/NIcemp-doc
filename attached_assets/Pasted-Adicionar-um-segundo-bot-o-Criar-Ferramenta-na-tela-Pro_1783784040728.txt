Adicionar um segundo botão "Criar Ferramenta" na tela Prompts Replit (NicEmp Docs), ao lado do
"Criar Página" já existente, específico pra criar uma nova calculadora/ferramenta no site nicemp.com.

CONTEXTO (convenções reais do nicemp.com — projeto artifacts/nicemp/)
- Rotas ficam em src/constants/routes.ts (objeto `routes`, chave: caminho).
- Roteador é o wouter (não react-router-dom). Rotas registradas em src/App.tsx como
  <Route path={routes.chave} component={NomeCalculator} />.
- Toda ferramenta pública segue o padrão: <Header /> + <main> com useSeo({ title, description,
  canonicalPath }) no topo do componente + conteúdo da calculadora + <FaqSection items={faqs} />
  opcional + <Footer />. Usar como referência de estilo/estrutura arquivos existentes como
  src/pages/PorcentagemCalculator.tsx ou src/pages/AreaPerimetroCalculator.tsx (não copiar
  conteúdo, só a estrutura/padrão visual).
- O catálogo principal de ferramentas é o array `allTools` em src/pages/ToolsHome.tsx, com os
  campos: title, description, href, icon (componente lucide-react), category
  ("Financeiro" | "Tributário" | "Matemática" | "Gestão" | nova categoria se fizer sentido),
  popular? (opcional), comingSoon? (opcional).
- IMPORTANTE — catálogo NÃO é centralizado: src/components/ToolsSection.tsx (destaques da home)
  e src/components/ToolsCarousel.tsx (carrossel da home) têm listas MANUAIS e SEPARADAS do
  allTools, cada uma com sua própria estrutura de dados. Nem toda ferramenta precisa estar nessas
  duas — são só destaques.

O QUE FAZER

1. Criar src/services/prompts/ToolCreationPromptGenerator.ts com:
     buildToolCreationPrompt(input: {
       toolName: string;          // ex: "Calculadora de Depreciação"
       routeKey: string;          // ex: "depreciacao" (chave em routes.ts)
       routePath: string;         // ex: "/ferramentas/calculadora-de-depreciacao"
       category: 'Financeiro' | 'Tributário' | 'Matemática' | 'Gestão';
       iconName: string;          // nome de ícone lucide-react, ex: "Calculator"
       featureOnHome: boolean;    // se true, inclui passo pra adicionar em
                                  // ToolsSection.tsx e ToolsCarousel.tsx também
       objective: string;        // o que a ferramenta calcula/faz
     }): string
   Seções do prompt gerado (mesmo estilo dos outros prompts do sistema):
   - "1. Objetivo" — nome + objective.
   - "2. Passo a passo obrigatório", nesta ordem:
     a) Adicionar a chave em routes.ts: `routeKey: '${routePath}',`
     b) Criar src/pages/${PascalCase}Calculator.tsx seguindo o padrão descrito acima
        (Header/main/useSeo/Footer, FaqSection se fizer sentido pro tema).
     c) Importar e adicionar UMA linha <Route path={routes.routeKey} component={X} /> em App.tsx,
        sem remover ou reordenar as rotas existentes.
     d) Adicionar UM objeto novo no array allTools de ToolsHome.tsx, com os campos title/description/
        href/icon/category (e popular/comingSoon se fizer sentido), sem remover nem reordenar os
        itens já existentes.
     e) SE featureOnHome for true: adicionar também UMA entrada em ToolsSection.tsx